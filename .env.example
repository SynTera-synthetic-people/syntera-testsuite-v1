ENVIRONMENT=development
DEBUG=false
# Local dev: SQLite (no PostgreSQL required). For production/docker use PostgreSQL.
DATABASE_URL=sqlite:///./syntera.db
# DATABASE_URL=postgresql://syntera:password@localhost:5432/syntera
SECRET_KEY=your-secret-key-change-this
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
BATCH_SIZE=32
NUM_WORKERS=4
LOG_LEVEL=INFO

# Optional: for Market Research Reverse Engineering (AI-powered questionnaire reconstruction)
# Use either OpenAI or Anthropic (Claude). Copy to .env and add your real key.
# OpenAI: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4o
# Anthropic (Claude Sonnet): https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-your-key-here
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
# Try OpenAI first to save Claude credits (set false to use Claude first)
# OPENAI_FIRST=true

# Optional: S3 industry surveys (Industry Survey Reports tab)
# Bucket: https://model-training1.s3.ap-south-1.amazonaws.com/Dat_for_model_Training/
#
# Private bucket (required for listing and download links):
# Create an IAM user with s3:ListBucket and s3:GetObject on this bucket, then set:
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=ap-south-1
# S3_INDUSTRY_BUCKET=model-training1
# S3_INDUSTRY_PREFIX=Dat_for_model_Training/
